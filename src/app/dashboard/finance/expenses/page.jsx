import DashboardLayout from "@/components/dashboard/DashboardLayout"; import { requireBusinessPermission } from "@/lib/business-context"; import { db } from "@/lib/db"; import { money } from "@/lib/finance-period"; import { hasPermission, PERMISSIONS } from "@/lib/permissions"; import { createExpenseAction } from "../actions";
export default async function ExpensesPage(){ const {businessId,business,businessRole}=await requireBusinessPermission(PERMISSIONS.FINANCE_VIEW); const canManageFinance=hasPermission(businessRole,PERMISSIONS.FINANCE_MANAGE); const [categories,expenses]=await Promise.all([db.expenseCategory.findMany({where:{businessId,isActive:true},orderBy:{name:"asc"}}),db.businessExpense.findMany({where:{businessId},include:{category:true},orderBy:{expenseDate:"desc"},take:200})]); return <DashboardLayout><div className="space-y-7"><div><p className="text-sm font-bold text-blue-600">Financa</p><h1 className="mt-2 text-3xl font-bold">Shpenzimet e biznesit</h1></div>{canManageFinance?<form action={createExpenseAction} className="grid gap-4 rounded-2xl border bg-white p-6 shadow-sm md:grid-cols-2 xl:grid-cols-4"><input name="description" required placeholder="Përshkrimi" className="rounded-xl border px-3 py-2"/><input name="amount" type="number" min="0.01" step="0.01" required placeholder="Shuma" className="rounded-xl border px-3 py-2"/><input name="expenseDate" type="date" required defaultValue={new Date().toISOString().slice(0,10)} className="rounded-xl border px-3 py-2"/><select name="paymentMethod" className="rounded-xl border px-3 py-2"><option value="CASH">Cash</option><option value="BANK_TRANSFER">Transfertë bankare</option><option value="CARD">Kartë</option><option value="OTHER">Tjetër</option></select><select name="categoryId" className="rounded-xl border px-3 py-2"><option value="">Pa kategori</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input name="newCategory" placeholder="Kategori e re (opsionale)" className="rounded-xl border px-3 py-2"/><input name="supplier" placeholder="Furnitori" className="rounded-xl border px-3 py-2"/><input name="documentNumber" placeholder="Nr. dokumenti" className="rounded-xl border px-3 py-2"/><textarea name="notes" placeholder="Shënime" className="rounded-xl border px-3 py-2 md:col-span-2"/><button className="rounded-xl bg-blue-600 px-4 py-2 font-bold text-white">Regjistro shpenzimin</button></form>:null}<div className="overflow-x-auto rounded-2xl border bg-white shadow-sm"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-4">Data</th><th>Përshkrimi</th><th>Kategoria</th><th>Furnitori</th><th>Mënyra</th><th className="pr-4 text-right">Shuma</th></tr></thead><tbody>{expenses.map(e=><tr key={e.id} className="border-t"><td className="p-4">{new Date(e.expenseDate).toLocaleDateString("sq-AL")}</td><td>{e.description}</td><td>{e.category?.name||"—"}</td><td>{e.supplier||"—"}</td><td>{e.paymentMethod}</td><td className="pr-4 text-right font-bold">{money(e.amount,business.currency)}</td></tr>)}</tbody></table></div></div></DashboardLayout> }
