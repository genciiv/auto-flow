import { notFound } from "next/navigation"; import DashboardLayout from "@/components/dashboard/DashboardLayout"; import { requireBusinessContext } from "@/lib/business-context"; import { db } from "@/lib/db"; import { hasPermission, PERMISSIONS } from "@/lib/permissions"; import { approveInventoryCountAction, postInventoryCountAction, saveInventoryCountAction, submitInventoryCountAction } from "../../actions";
export default async function CountDetail({params}){ const {id}=await params; const {businessId,businessRole}=await requireBusinessContext(); const count=await db.inventoryCount.findFirst({where:{id,businessId},include:{items:{orderBy:{partName:"asc"}}}}); if(!count) notFound(); const editable=count.status==="DRAFT"&&hasPermission(businessRole,PERMISSIONS.INVENTORY_COUNTS_MANAGE), canApprove=hasPermission(businessRole,PERMISSIONS.INVENTORY_COUNTS_APPROVE); const totals=count.items.reduce((a,i)=>({expected:a.expected+i.expectedValue,actual:a.actual+Number(i.actualValue||0),diff:a.diff+Number(i.differenceValue||0)}),{expected:0,actual:0,diff:0}); return <DashboardLayout><div className="space-y-6"><div><p className="text-sm font-bold text-blue-600">{count.status}</p><h1 className="mt-2 text-3xl font-bold">{count.name}</h1><p className="mt-2 text-slate-500">Data: {new Date(count.countDate).toLocaleDateString("sq-AL")}</p></div><div className="grid gap-4 md:grid-cols-3">{[["Vlera sipas sistemit",totals.expected],["Vlera reale",totals.actual],["Diferenca",totals.diff]].map(([l,v])=><div key={l} className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{l}</p><p className="mt-2 text-2xl font-bold">{v.toLocaleString("sq-AL")} ALL</p></div>)}</div><form action={saveInventoryCountAction} className="overflow-x-auto rounded-2xl border bg-white"><input type="hidden" name="inventoryCountId" value={count.id}/><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Pjesa</th><th>Pritet</th><th>Reale</th><th>Diferenca</th><th>Vlera diferencës</th><th>Shënim</th></tr></thead><tbody>{count.items.map(i=><tr key={i.id} className="border-t"><td className="p-3 font-semibold">{i.partName}<div className="text-xs text-slate-400">{i.partCode||""}</div></td><td>{i.expectedQuantity}</td><td>{editable?<input type="number" min="0" name={`actual_${i.id}`} defaultValue={i.actualQuantity??i.expectedQuantity} className="w-24 rounded-lg border px-2 py-1"/>:(i.actualQuantity??"—")}</td><td>{i.difference??"—"}</td><td>{Number(i.differenceValue||0).toLocaleString("sq-AL")} ALL</td><td>{editable?<input name={`note_${i.id}`} defaultValue={i.note||""} className="rounded-lg border px-2 py-1"/>:(i.note||"—")}</td></tr>)}</tbody></table>{editable?<div className="flex gap-3 border-t p-4"><button className="rounded-xl bg-slate-950 px-4 py-2 font-bold text-white">Ruaj sasitë</button></div>:null}</form><div className="flex flex-wrap gap-3">{editable?<form action={submitInventoryCountAction}><input type="hidden" name="inventoryCountId" value={count.id}/><button className="rounded-xl bg-blue-600 px-4 py-2 font-bold text-white">Dërgo për shqyrtim</button></form>:null}{count.status==="IN_REVIEW"&&canApprove?<form action={approveInventoryCountAction}><input type="hidden" name="inventoryCountId" value={count.id}/><button className="rounded-xl bg-emerald-600 px-4 py-2 font-bold text-white">Aprovo</button></form>:null}{count.status==="APPROVED"&&canApprove?<form action={postInventoryCountAction}><input type="hidden" name="inventoryCountId" value={count.id}/><button className="rounded-xl bg-amber-600 px-4 py-2 font-bold text-white">Posto korrigjimet në stok</button></form>:null}</div></div></DashboardLayout> }
