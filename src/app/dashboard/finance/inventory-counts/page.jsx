import Link from "next/link"; import DashboardLayout from "@/components/dashboard/DashboardLayout"; import { requireBusinessPermission } from "@/lib/business-context"; import { db } from "@/lib/db"; import { PERMISSIONS } from "@/lib/permissions"; import { createInventoryCountAction } from "../actions";
const labels={DRAFT:"Draft",IN_REVIEW:"Në shqyrtim",APPROVED:"Aprovuar",POSTED:"Postuar",CANCELLED:"Anuluar"}; export default async function CountsPage(){ const {businessId}=await requireBusinessPermission(PERMISSIONS.INVENTORY_COUNTS_VIEW); const rows=await db.inventoryCount.findMany({where:{businessId},include:{_count:{select:{items:true}}},orderBy:{countDate:"desc"}}); return <DashboardLayout><div className="space-y-7"><div><p className="text-sm font-bold text-blue-600">Inventari financiar</p><h1 className="mt-2 text-3xl font-bold">Inventarizimet periodike</h1><p className="mt-2 text-slate-500">Krahaso stokun në sistem me sasinë reale dhe posto korrigjimet vetëm pas aprovimit.</p></div><form action={createInventoryCountAction} className="grid gap-4 rounded-2xl border bg-white p-6 md:grid-cols-4"><input name="name" required placeholder="P.sh. Inventarizim Gusht 2026" className="rounded-xl border px-3 py-2"/><select name="periodType" className="rounded-xl border px-3 py-2"><option value="MONTHLY">Mujor</option><option value="QUARTERLY">3-mujor</option><option value="SIX_MONTHS">6-mujor</option><option value="YEARLY">Vjetor</option><option value="CUSTOM">Personalizuar</option></select><input name="countDate" type="date" required defaultValue={new Date().toISOString().slice(0,10)} className="rounded-xl border px-3 py-2"/><button className="rounded-xl bg-blue-600 px-4 py-2 font-bold text-white">Krijo inventarizimin</button></form><div className="grid gap-4">{rows.map(r=><Link key={r.id} href={`/dashboard/finance/inventory-counts/${r.id}`} className="rounded-2xl border bg-white p-5 shadow-sm hover:border-blue-300"><div className="flex justify-between"><div><h2 className="font-bold">{r.name}</h2><p className="mt-1 text-sm text-slate-500">{new Date(r.countDate).toLocaleDateString("sq-AL")} · {r._count.items} artikuj</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{labels[r.status]}</span></div></Link>)}</div></div></DashboardLayout> }
